import 'package:flutter/material.dart';

class GoogleMapScren extends StatelessWidget {
  const GoogleMapScren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
